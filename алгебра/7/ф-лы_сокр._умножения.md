---
layout: directory
title: Ф-лы сокр. умножения
---

{% for p in site.pages %}
  {{ page.path }}
  {{ page.url }}
  <hr />
  {{ p.dir }}
  {{ p.id }}
  <hr />
  <hr />
  {% if p.dir == page.id %}
    {{p.title}}
    {{p.url}}
  {% endif %}
{% endfor %}
