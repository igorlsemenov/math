---
layout: directory
title: Ф-лы сокр. умножения
---

{% for p in site.pages %}
  {{ page.id }}
  {{ p.dir }}
  {{ p.id }}
  <hr />
  {% if p.dir == page.id %}
    {{p.title}}
    {{p.url}}
  {% endif %}
{% endfor %}
